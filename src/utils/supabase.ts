export async function saveEmailSignup(email: string, agreedToTos: boolean, selectedPlan: string) {
  // Mock implementation - replace with actual Supabase integration when ready
  return {
    success: true,
    duplicate: false,
    message: 'Email saved successfully'
  };
}